{
  "name": "database_project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"cd ./server && npm start\" \"cd ./client && npm start\"",
    "dev": "concurrently \"cd ./server && npm install && npm run dev\" \"cd ./client && npm install && npm run dev\"",
    "build": "concurrently \"cd ./server && npm install\" \"cd ./client && npm install && npm run build\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
